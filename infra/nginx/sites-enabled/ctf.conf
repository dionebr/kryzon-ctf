# CTF Platform - Main Site
server {
    listen 80;
    server_name ctf.local www.ctf.local;

    # Frontend (React)
    location / {
        proxy_pass http://frontend:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Backend API
    location /api/ {
        proxy_pass http://backend:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Challenge instances - dynamic routing template
# Individual challenge instances will be created dynamically
# Format: vm-<instance-id>.ctf.local

# Example for challenge instances (will be generated dynamically by backend)
# server {
#     listen 80;
#     server_name vm-*.ctf.local;
#     
#     location / {
#         # This will be configured dynamically by the backend
#         # proxy_pass http://challenge-container:port;
#         return 503 "Challenge instance not configured";
#     }
# }